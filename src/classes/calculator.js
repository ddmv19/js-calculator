export class Calculator {
  constructor() {
    this.array = [];
  }

  sum(a, b) {
    return a + b; 
  }

  subtrack(a, b) {
    return a - b;
  }

  multiplication(a, b) {
    return a * b;
  }

  division(a, b) {
    return a / b;
  }
}